@file:Suppress("UNUSED_PARAMETER", "PackageDirectoryMismatch", "unused")
package android

/**
 * Offline check stub for the framework resource class `android.R` (the real one
 * ships inside android.jar). Only ids referenced by this app are modelled.
 */
object R {
    object drawable {
        const val stat_sys_download_done = 1
        const val stat_sys_download = 2
        const val ic_media_play = 3
        const val ic_media_pause = 4
        const val ic_menu_close_clear_cancel = 5
    }
}

// (see AndroidFramework.kt for the rationale)
// (android.content.pm/res, android.database, android.graphics, android.provider).
