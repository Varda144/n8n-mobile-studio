# Workflow editor

The editor stores a generic `WorkflowGraph` with nodes and connections. The mobile editor is gesture-first: pan/drag, touch-friendly controls, zoom and an editable node representation. Future phases add node search, configuration sheets, connection handles, minimap, undo/redo and JSON round-tripping.

Unknown node properties must be retained when importing/exporting workflow JSON.
